{
  "name": "parsashop",
  "version": "1.0.0",
  "description": "1. git push",
  "repository": {
    "type": "git",
    "url": "(https://github.com/RezaeiArshad/parsashop.git)"
  },
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "build": "cd backend && npm install && cd ../frontend && npm install && npm run build",
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
